{
  "name": "nexus-prisma",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "gen": "prisma generate",
    "dev": "ts-node-dev --tree-kill --prefer-ts true --no-notify -r tsconfig-paths/register --respawn --transpile-only index.ts",
    "prisma:studio": "prisma studio",
    "dev:migrate": "prisma migrate dev --preview-feature",
    "reset-db": "prisma migrate reset --force --preview-feature"
  },
  "dependencies": {
    "@prisma/client": "^3.2.1",
    "apollo-server-express": "^3.4.0",
    "express": "^4.17.1",
    "graphql": "^15.6.1",
    "nexus": "^1.1.0",
    "nexus-prisma": "^0.34.0"
  },
  "devDependencies": {
    "prisma": "^3.2.1",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.4.4"
  }
}
